# Waste AI Classifier 🌍

This project uses **AI and Computer Vision** to identify waste as either **Biodegradable** or **Non-Biodegradable** in real-time using a webcam.

## 🛠️ How it Works
- Built with **Python 3.13**.
- Uses **TensorFlow Lite** for fast AI processing.
- Uses **OpenCV** to access the webcam and show results on screen.

## 🚀 Setup
1. Install dependencies: `pip install -r requirements.txt`
2. Run the project: `python main.py`